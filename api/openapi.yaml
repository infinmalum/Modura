openapi: 3.0.3
info:
  title: Modura HTTP API
  version: 0.1.0
  description: Authoritative HTTP contract for the Modura modular monolith.
servers:
  - url: /api
paths:
  /livez:
    get:
      operationId: getLiveness
      summary: Report whether the process is alive
      tags: [system]
      responses:
        "200":
          description: Process is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /readyz:
    get:
      operationId: getReadiness
      summary: Report whether the process can serve traffic
      tags: [system]
      responses:
        "200":
          description: Process is ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
        "503":
          description: Process is not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /auth/login:
    post:
      operationId: login
      summary: Establish a tenant-local session
      tags: [authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Session established; the refresh secret is set as an HttpOnly cookie
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenResponse"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
  /platform/auth/login:
    post:
      operationId: platformLogin
      summary: Establish a global platform-administrator session
      tags: [platform-authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformLoginRequest"
      responses:
        "200":
          description: Platform session established with a distinct refresh cookie
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenResponse"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
  /platform/auth/refresh:
    post:
      operationId: platformRefresh
      summary: Rotate a platform-administrator refresh secret
      tags: [platform-authentication]
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "200":
          description: Platform session rotated
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenResponse"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /platform/auth/logout:
    post:
      operationId: platformLogout
      summary: Revoke the current platform-administrator session
      tags: [platform-authentication]
      security:
        - platformBearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "204":
          description: Platform session revoked and cookies cleared
          headers:
            Set-Cookie:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /platform/tenants:
    post:
      operationId: provisionPlatformTenant
      summary: Atomically provision a tenant as a global platform administrator
      tags: [platform-tenants]
      security:
        - platformBearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProvisionTenantRequest"
      responses:
        "201":
          description: Tenant, first administrator, organization, and role were created atomically
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisionTenantResponse"
        "200":
          description: The same completed provisioning request was returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisionTenantResponse"
        "400":
          description: Invalid provisioning request
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
        "409":
          description: Idempotency key was reused with different input
    get:
      operationId: listPlatformTenants
      summary: List tenants as a global platform administrator
      tags: [platform-tenants]
      security:
        - platformBearerAuth: []
      responses:
        "200":
          description: Tenant summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlatformTenant"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
  /platform/tenants/{tenantId}/suspend:
    post:
      operationId: suspendPlatformTenant
      summary: Suspend an active tenant with auditable reason
      tags: [platform-tenants]
      security:
        - platformBearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantId"
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TenantLifecycleRequest"
      responses:
        "204":
          description: Tenant suspended and audit evidence committed
        "400":
          description: Invalid lifecycle transition
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
        "404":
          description: Tenant not found
  /platform/tenants/{tenantId}/reactivate:
    post:
      operationId: reactivatePlatformTenant
      summary: Reactivate a suspended tenant with auditable reason
      tags: [platform-tenants]
      security:
        - platformBearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantId"
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TenantLifecycleRequest"
      responses:
        "204":
          description: Tenant reactivated and audit evidence committed
        "400":
          description: Invalid lifecycle transition
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
        "404":
          description: Tenant not found
  /platform/settings/dictionaries:
    get:
      operationId: listPlatformDictionaries
      summary: List global dictionaries as a platform administrator
      tags: [platform-settings]
      security: [{ platformBearerAuth: [] }]
      responses:
        "200":
          description: Global dictionaries
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Dictionary" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
  /platform/settings/dictionaries/{dictionaryCode}:
    put:
      operationId: replacePlatformDictionary
      summary: Replace a complete global dictionary
      tags: [platform-settings]
      security: [{ platformBearerAuth: [] }]
      parameters:
        - { $ref: "#/components/parameters/DictionaryCode" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplacePlatformDictionaryRequest" }
      responses:
        "200":
          description: Global dictionary desired state stored
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Dictionary" }
        "400": { description: Invalid request }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/CsrfFailed" }
        "409": { description: Version conflict }
  /platform/settings/configurations:
    get:
      operationId: listPlatformConfigurations
      summary: List global non-secret configuration definitions and defaults
      tags: [platform-settings]
      security: [{ platformBearerAuth: [] }]
      responses:
        "200":
          description: Global configurations
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Configuration" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
  /platform/settings/configurations/{configurationKey}:
    put:
      operationId: putPlatformConfiguration
      summary: Create or update a global non-secret configuration
      tags: [platform-settings]
      security: [{ platformBearerAuth: [] }]
      parameters:
        - { $ref: "#/components/parameters/ConfigurationKey" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PutPlatformConfigurationRequest" }
      responses:
        "200":
          description: Global configuration stored
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Configuration" }
        "400": { description: Invalid request }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/CsrfFailed" }
        "409": { description: Version conflict }
  /auth/refresh:
    post:
      operationId: refresh
      summary: Rotate the refresh secret and issue an access token
      tags: [authentication]
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "200":
          description: Session rotated
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenResponse"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      tags: [authentication]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "204":
          description: Session revoked and refresh cookie cleared
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /auth/logout-all:
    post:
      operationId: logoutAll
      summary: Revoke every session for the authenticated tenant-local user
      tags: [authentication]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "204":
          description: All sessions revoked and refresh cookie cleared
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /auth/password:
    put:
      operationId: changePassword
      summary: Change the current user's password and rotate its session
      tags: [authentication]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordRequest"
      responses:
        "200":
          description: Password changed, other sessions revoked, and current session rotated
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessTokenResponse"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/CsrfFailed"
  /auth/password-resets:
    post:
      operationId: resetPassword
      summary: Consume a password-reset token
      tags: [authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OneTimeCredentialRequest"
      responses:
        "204":
          description: Password replaced and all sessions revoked
        "400":
          description: Password policy or request validation failed
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
  /auth/invitations/accept:
    post:
      operationId: acceptInvitation
      summary: Consume an invitation and establish the first password
      tags: [authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OneTimeCredentialRequest"
      responses:
        "204":
          description: Invitation accepted and local account activated
        "400":
          description: Password policy or request validation failed
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
  /organization/departments:
    get:
      operationId: listDepartments
      summary: List departments in the authenticated tenant
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.departments, action: read }
      responses:
        "200":
          description: Tenant department list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Department"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
    post:
      operationId: createDepartment
      summary: Create a department in the authenticated tenant
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.departments, action: create }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDepartmentRequest"
      responses:
        "201":
          description: Department created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentifierResponse"
        "400":
          description: Invalid department input
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
  /organization/departments/{departmentId}:
    parameters:
      - $ref: "#/components/parameters/DepartmentId"
    patch:
      operationId: moveDepartment
      summary: Move a department under another parent
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.departments, action: update }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveDepartmentRequest"
      responses:
        "204":
          description: Department moved
        "400":
          description: Move violates a department invariant
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
        "404":
          description: Department or parent not found in the tenant
    put:
      operationId: updateDepartment
      summary: Update a department name and sort order
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.departments, action: update }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDepartmentRequest"
      responses:
        "204":
          description: Department updated
        "400":
          description: Invalid or duplicate department
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
        "404":
          description: Department not found in the tenant
    delete:
      operationId: deleteDepartment
      summary: Delete an unused non-root department
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.departments, action: delete }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      responses:
        "204":
          description: Department deleted
        "400":
          description: Root or referenced department cannot be deleted
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
        "404":
          description: Department not found in the tenant
  /organization/positions:
    get:
      operationId: listPositions
      summary: List positions in the authenticated tenant
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.positions, action: read }
      responses:
        "200":
          description: Tenant position list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Position"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
    post:
      operationId: createPosition
      summary: Create a position in the authenticated tenant
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.positions, action: create }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePositionRequest"
      responses:
        "201":
          description: Position created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentifierResponse"
        "400":
          description: Invalid or duplicate position
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
  /organization/positions/{positionId}:
    parameters:
      - $ref: "#/components/parameters/PositionId"
    patch:
      operationId: updatePosition
      summary: Update a position name and status
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.positions, action: update }
      parameters:
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePositionRequest"
      responses:
        "204":
          description: Position updated
        "400":
          description: Invalid or duplicate position
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
        "404":
          description: Position not found in the tenant
  /organization/users/{userId}/assignment:
    put:
      operationId: assignUserOrganization
      summary: Set a user's primary department and optional position
      tags: [organization]
      security:
        - bearerAuth: []
      x-modura-permission: { resource: organization.user-organization, action: update }
      parameters:
        - $ref: "#/components/parameters/UserId"
        - $ref: "#/components/parameters/CsrfToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignUserOrganizationRequest"
      responses:
        "204":
          description: User organization assignment replaced
        "400":
          description: Invalid or cross-tenant assignment
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/AuthenticationFailed"
        "403":
          $ref: "#/components/responses/AuthorizationFailed"
  /authorization/roles:
    get:
      operationId: listRoles
      summary: List roles in the authenticated tenant
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.roles, action: read }
      responses:
        "200":
          description: Tenant role list
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Role" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
    post:
      operationId: createRole
      summary: Create an empty non-reserved tenant role
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.roles, action: create }
      parameters: [{ $ref: "#/components/parameters/CsrfToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRoleRequest" }
      responses:
        "201":
          description: Role created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Role" }
        "400": { description: Invalid or duplicate role }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
  /authorization/permissions:
    get:
      operationId: listEffectivePermissions
      summary: List canonical permissions granted to the authenticated tenant actor
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Effective canonical permission list
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/EffectivePermission" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
  /authorization/roles/{roleId}/policies:
    get:
      operationId: getRolePolicySet
      summary: Read a role's versioned policy desired state
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.policies, action: read }
      parameters:
        - { $ref: "#/components/parameters/RoleId" }
      responses:
        "200":
          description: Current role policy desired state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RolePolicySet" }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: Role not found in the tenant }
    put:
      operationId: replaceRolePolicies
      summary: Replace a non-reserved role's policy desired state
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.policies, action: update }
      parameters:
        - { $ref: "#/components/parameters/RoleId" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplaceRolePoliciesRequest" }
      responses:
        "200":
          description: Policy desired state replaced
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VersionResponse" }
        "400": { description: Invalid policy desired state }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: Role not found in the tenant }
        "409": { description: Expected role version is stale }
  /authorization/users/{userId}/roles:
    get:
      operationId: getUserRoleGrants
      summary: Read a user's versioned role desired state
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.user-roles, action: read }
      parameters: [{ $ref: "#/components/parameters/UserId" }]
      responses:
        "200":
          description: Current user role grants
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserRoleGrantSet" }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: User not found in the tenant }
    put:
      operationId: replaceUserRoleGrants
      summary: Replace a user's non-reserved role grants with optimistic locking
      tags: [authorization]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: authorization.user-roles, action: update }
      parameters:
        - { $ref: "#/components/parameters/UserId" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplaceUserRoleGrantsRequest" }
      responses:
        "200":
          description: User role grants replaced
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserRoleGrantSet" }
        "400": { description: Invalid desired state }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: User not found in the tenant }
        "409": { description: Expected grant version is stale }
  /settings/dictionaries:
    get:
      operationId: listDictionaries
      summary: List effective tenant-over-global dictionaries
      tags: [settings]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: settings.dictionaries, action: read }
      responses:
        "200":
          description: Effective dictionary list
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Dictionary" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
  /settings/dictionaries/{dictionaryCode}:
    put:
      operationId: replaceDictionary
      summary: Create or replace a complete tenant dictionary
      tags: [settings]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: settings.dictionaries, action: update }
      parameters:
        - { $ref: "#/components/parameters/DictionaryCode" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplaceDictionaryRequest" }
      responses:
        "200":
          description: Dictionary desired state stored
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Dictionary" }
        "400": { description: Invalid dictionary desired state }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: Tenant dictionary not found for an update }
        "409": { description: Expected dictionary version is stale }
    delete:
      operationId: deleteDictionary
      summary: Delete a tenant dictionary and reveal any global fallback
      tags: [settings]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: settings.dictionaries, action: delete }
      parameters:
        - { $ref: "#/components/parameters/DictionaryCode" }
        - { $ref: "#/components/parameters/ExpectedVersion" }
        - { $ref: "#/components/parameters/CsrfToken" }
      responses:
        "204": { description: Tenant dictionary deleted }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: Tenant dictionary not found }
        "409": { description: Expected dictionary version is stale }
  /settings/configurations:
    get:
      operationId: listConfigurations
      summary: List effective non-secret configuration
      tags: [settings]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: settings.configurations, action: read }
      responses:
        "200":
          description: Effective configuration list
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Configuration" } }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
  /settings/configurations/{configurationKey}:
    put:
      operationId: putConfiguration
      summary: Create or replace an eligible tenant configuration override
      tags: [settings]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: settings.configurations, action: update }
      parameters:
        - { $ref: "#/components/parameters/ConfigurationKey" }
        - { $ref: "#/components/parameters/CsrfToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PutConfigurationRequest" }
      responses:
        "200":
          description: Tenant configuration override stored
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Configuration" }
        "400": { description: Value does not match the declared type }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
        "404": { description: Configuration definition not found }
        "409": { description: Expected configuration version is stale }
  /audit/events:
    get:
      operationId: listAuditEvents
      summary: List redacted audit events in the authenticated tenant
      tags: [audit]
      security: [{ bearerAuth: [] }]
      x-modura-permission: { resource: audit.events, action: read }
      parameters:
        - name: action
          in: query
          schema: { type: string, maxLength: 128 }
        - name: resource
          in: query
          schema: { type: string, maxLength: 128 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - name: offset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Newest-first bounded tenant audit page
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/AuditEvent" } }
        "400": { description: Invalid audit filter or page }
        "401": { $ref: "#/components/responses/AuthenticationFailed" }
        "403": { $ref: "#/components/responses/AuthorizationFailed" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    platformBearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    CsrfToken:
      name: X-CSRF-Token
      in: header
      required: true
      schema:
        type: string
        minLength: 32
        maxLength: 256
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        format: uuid
    DepartmentId:
      name: departmentId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PositionId:
      name: positionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    UserId:
      name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    RoleId:
      name: roleId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DictionaryCode:
      name: dictionaryCode
      in: path
      required: true
      schema: { type: string, minLength: 1, maxLength: 64 }
    ConfigurationKey:
      name: configurationKey
      in: path
      required: true
      schema: { type: string, minLength: 1, maxLength: 64 }
    ExpectedVersion:
      name: expectedVersion
      in: query
      required: true
      schema: { type: integer, format: int64, minimum: 1 }
    TenantId:
      name: tenantId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    AuthenticationFailed:
      description: Tenant or credentials are invalid, or the session is unavailable
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    CsrfFailed:
      description: CSRF validation failed
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    AuthorizationFailed:
      description: The authenticated actor lacks the required permission
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
  schemas:
    LoginRequest:
      type: object
      additionalProperties: false
      required: [tenant, login, password]
      properties:
        tenant:
          type: string
          minLength: 1
          maxLength: 63
        login:
          type: string
          minLength: 1
          maxLength: 254
        password:
          type: string
          minLength: 1
          maxLength: 1024
          writeOnly: true
    PlatformLoginRequest:
      type: object
      additionalProperties: false
      required: [username, password]
      properties:
        username:
          type: string
          minLength: 1
          maxLength: 254
        password:
          type: string
          minLength: 1
          maxLength: 1024
          writeOnly: true
    PlatformTenant:
      type: object
      additionalProperties: false
      required: [id, slug, displayName, status, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
        displayName:
          type: string
        status:
          type: string
          enum: [provisioning, active, suspended]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ProvisionTenantRequest:
      type: object
      additionalProperties: false
      required: [slug, displayName, rootDepartmentName, administratorUsername, reason]
      properties:
        slug:
          type: string
          minLength: 1
          maxLength: 63
          pattern: "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$"
        displayName:
          type: string
          minLength: 1
          maxLength: 128
        rootDepartmentName:
          type: string
          minLength: 1
          maxLength: 128
        administratorUsername:
          type: string
          minLength: 1
          maxLength: 128
        administratorEmail:
          type: string
          format: email
          maxLength: 254
        reason:
          type: string
          minLength: 1
          maxLength: 500
    ProvisionTenantResponse:
      type: object
      additionalProperties: false
      required: [tenantId, created]
      properties:
        tenantId:
          type: string
          format: uuid
        created:
          type: boolean
    TenantLifecycleRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
    ChangePasswordRequest:
      type: object
      additionalProperties: false
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          type: string
          minLength: 1
          maxLength: 1024
          writeOnly: true
        newPassword:
          type: string
          minLength: 12
          maxLength: 1024
          writeOnly: true
    OneTimeCredentialRequest:
      type: object
      additionalProperties: false
      required: [token, newPassword]
      properties:
        token:
          type: string
          minLength: 32
          maxLength: 512
          writeOnly: true
        newPassword:
          type: string
          minLength: 12
          maxLength: 1024
          writeOnly: true
    Department:
      type: object
      additionalProperties: false
      required: [id, name, sortOrder]
      properties:
        id:
          type: string
          format: uuid
        parentId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        sortOrder:
          type: integer
    CreateDepartmentRequest:
      type: object
      additionalProperties: false
      required: [parentId, name, sortOrder]
      properties:
        parentId:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 128
        sortOrder:
          type: integer
          minimum: -2147483648
          maximum: 2147483647
    MoveDepartmentRequest:
      type: object
      additionalProperties: false
      required: [parentId]
      properties:
        parentId:
          type: string
          format: uuid
    UpdateDepartmentRequest:
      type: object
      additionalProperties: false
      required: [name, sortOrder]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
        sortOrder:
          type: integer
          minimum: -2147483648
          maximum: 2147483647
    IdentifierResponse:
      type: object
      additionalProperties: false
      required: [id]
      properties:
        id:
          type: string
          format: uuid
    Position:
      type: object
      additionalProperties: false
      required: [id, name, status]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        status:
          type: string
          enum: [active, disabled]
    CreatePositionRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
    UpdatePositionRequest:
      type: object
      additionalProperties: false
      required: [name, status]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
        status:
          type: string
          enum: [active, disabled]
    AssignUserOrganizationRequest:
      type: object
      additionalProperties: false
      required: [departmentId]
      properties:
        departmentId:
          type: string
          format: uuid
        positionId:
          type: string
          format: uuid
          nullable: true
    Role:
      type: object
      additionalProperties: false
      required: [id, code, name, reserved, version]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        reserved: { type: boolean }
        version: { type: integer, format: int64, minimum: 1 }
    EffectivePermission:
      type: object
      additionalProperties: false
      required: [resource, action]
      properties:
        resource: { type: string }
        action: { type: string }
    CreateRoleRequest:
      type: object
      additionalProperties: false
      required: [code, name]
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 63
          pattern: "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$"
        name: { type: string, minLength: 1, maxLength: 128 }
    DataScopeKind:
      type: string
      enum: [all, self, department, department-and-descendants, custom]
    RolePolicy:
      type: object
      additionalProperties: false
      required: [resource, action, dataScope]
      properties:
        resource:
          type: string
          enum: [organization.departments, organization.positions, organization.user-organization, authorization.roles, authorization.policies, authorization.user-roles, settings.dictionaries, settings.configurations, audit.events]
        action:
          type: string
          enum: [read, create, update, delete]
        dataScope: { $ref: "#/components/schemas/DataScopeKind" }
        departmentIds:
          type: array
          uniqueItems: true
          items: { type: string, format: uuid }
    ReplaceRolePoliciesRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, policies]
      properties:
        expectedVersion: { type: integer, format: int64, minimum: 1 }
        policies:
          type: array
          maxItems: 24
          items: { $ref: "#/components/schemas/RolePolicy" }
    RolePolicySet:
      type: object
      additionalProperties: false
      required: [version, reserved, policies]
      properties:
        version: { type: integer, format: int64, minimum: 1 }
        reserved: { type: boolean }
        policies:
          type: array
          items: { $ref: "#/components/schemas/RolePolicy" }
    VersionResponse:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: { type: integer, format: int64, minimum: 1 }
    UserRoleGrantSet:
      type: object
      additionalProperties: false
      required: [version, roleIds]
      properties:
        version: { type: integer, format: int64, minimum: 1 }
        roleIds:
          type: array
          uniqueItems: true
          items: { type: string, format: uuid }
    ReplaceUserRoleGrantsRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, roleIds]
      properties:
        expectedVersion: { type: integer, format: int64, minimum: 1 }
        roleIds:
          type: array
          uniqueItems: true
          items: { type: string, format: uuid }
    SettingSource:
      type: string
      enum: [global, tenant]
    ConfigurationValueType:
      type: string
      enum: [string, boolean, integer, json]
    DictionaryItem:
      type: object
      additionalProperties: false
      required: [code, label, sortOrder, enabled]
      properties:
        code: { type: string, minLength: 1, maxLength: 64 }
        label: { type: string, minLength: 1, maxLength: 128 }
        sortOrder: { type: integer }
        enabled: { type: boolean }
    Dictionary:
      type: object
      additionalProperties: false
      required: [code, name, source, version, items]
      properties:
        code: { type: string }
        name: { type: string }
        source: { $ref: "#/components/schemas/SettingSource" }
        version: { type: integer, format: int64, minimum: 1 }
        items:
          type: array
          items: { $ref: "#/components/schemas/DictionaryItem" }
    ReplaceDictionaryRequest:
      type: object
      additionalProperties: false
      required: [name, expectedVersion, items]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        expectedVersion: { type: integer, format: int64, minimum: 0 }
        items:
          type: array
          maxItems: 500
          items: { $ref: "#/components/schemas/DictionaryItem" }
    ReplacePlatformDictionaryRequest:
      type: object
      additionalProperties: false
      required: [name, expectedVersion, items, reason]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        expectedVersion: { type: integer, format: int64, minimum: 0 }
        items:
          type: array
          maxItems: 500
          items: { $ref: "#/components/schemas/DictionaryItem" }
        reason: { type: string, minLength: 1, maxLength: 500 }
    Configuration:
      type: object
      additionalProperties: false
      required: [key, name, valueType, tenantOverridable, source, version, value]
      properties:
        key: { type: string }
        name: { type: string }
        valueType: { $ref: "#/components/schemas/ConfigurationValueType" }
        tenantOverridable: { type: boolean }
        source: { $ref: "#/components/schemas/SettingSource" }
        version: { type: integer, format: int64, minimum: 1 }
        value: {}
    PutConfigurationRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, value]
      properties:
        expectedVersion: { type: integer, format: int64, minimum: 0 }
        value: {}
    PutPlatformConfigurationRequest:
      type: object
      additionalProperties: false
      required: [name, valueType, tenantOverridable, expectedVersion, value, reason]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        valueType: { $ref: "#/components/schemas/ConfigurationValueType" }
        tenantOverridable: { type: boolean }
        expectedVersion: { type: integer, format: int64, minimum: 0 }
        value: {}
        reason: { type: string, minLength: 1, maxLength: 500 }
    AuditEvent:
      type: object
      additionalProperties: false
      required: [id, actorType, actorId, tenantId, action, resource, resourceId, reason, result, correlationId, occurredAt]
      properties:
        id: { type: string, format: uuid }
        actorType:
          type: string
          enum: [platform_administrator, tenant_user]
        actorId: { type: string, format: uuid }
        tenantId: { type: string, format: uuid }
        action: { type: string }
        resource: { type: string }
        resourceId: { type: string, format: uuid }
        reason: { type: string }
        result:
          type: string
          enum: [succeeded, failed]
        correlationId: { type: string }
        occurredAt: { type: string, format: date-time }
        beforeState: {}
        afterState: {}
    AccessTokenResponse:
      type: object
      additionalProperties: false
      required: [accessToken, tokenType, expiresIn, csrfToken]
      properties:
        accessToken:
          type: string
        tokenType:
          type: string
          enum: [Bearer]
        expiresIn:
          type: integer
          format: int64
          minimum: 1
        csrfToken:
          type: string
    HealthStatus:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          enum: [ok]
    Problem:
      type: object
      additionalProperties: false
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
        traceId:
          type: string
